# 2026-04-04

## k8s-2025 Reorg Planning (5:00 AM CDT)

### Daily status report
- **What was reviewed today:** `docs/README.md`, `docs/network-audit-process.md`, `docs/opnsense-dns-dhcp-recommendations.md`, listings under `docs/guides/`, `docs/reference/`, `operations/audits/`, `ansible/playbooks/README.md`, `ansible/playbooks/` structure, and `ansible/awx/job-templates.yml`.
- **What changed in the planning docs:**
  - Updated all four planning docs to 2026-04-04.
  - Added a focused validation/refinement pass centered on the two remaining docs-root exceptions and the residual naming question.
  - Clarified recommended destinations for the two root-level docs:
    - `docs/network-audit-process.md` → preferably `docs/guides/` (or `operations/runbooks/` if day-2 procedures should live under operations)
    - `docs/opnsense-dns-dhcp-recommendations.md` → preferably `operations/audits/` (or `docs/reference/` if generated reference material should stay under docs)
  - Documented that `ansible/playbooks/README.md` still describes the old maintenance-centric layout and should be refreshed during any future execution pass.
  - Tightened the naming guidance to distinguish operator-facing names that need consistency from internal filenames that already gain clarity from the new intent directories.
- **Current milestone status:**
  - Phase 1 inventory/classification: complete enough for planning.
  - Phase 2 pain points/overlaps: complete enough; now mostly bounded to a few residual placement/documentation issues.
  - Phase 3 target structure: complete and already reflected in the live repo.
  - Phase 4 naming conventions: mature; active AWX naming looks good, and remaining filename cleanup should be selective.
  - Phase 5 transition sequence: effectively complete; only a tiny low-risk cleanup wave remains if desired.
- **Open questions / blockers:**
  1. Should runbook-style docs like `network-audit-process.md` stay under `docs/guides/` or move to an `operations/runbooks/` pattern later?
  2. Should generated analysis artifacts like `opnsense-dns-dhcp-recommendations.md` live under `operations/audits/` or stay under `docs/reference/`?
  3. Does Al want the repo to preserve pragmatic top-level exceptions (`scripts/`, `cloud-init-templates/`, `kubernetes/`) rather than forcing stricter taxonomy for its own sake?
- **Next recommended steps:**
  1. if doing another execution pass, move the two remaining docs-root files into their chosen homes
  2. update `docs/README.md` and `ansible/playbooks/README.md` to match the lived structure
  3. stop unless a real operator-facing naming/search problem is found
- **Proposal ready to present to Al?:** yes. It is ready now; remaining work is small refinement, not architecture design.

---
Reviewed/distilled into `MEMORY.md` on 2026-04-13.
