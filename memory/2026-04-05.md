# 2026-04-05 — Daily Log

> Reviewed and distilled into `MEMORY.md` on 2026-04-09 during daily memory maintenance.

---

## k8s-2025 Documentation Remediation Pass (3:55 PM CDT)

- Al asked for a full documentation review of `k8s-2025`, then approved execution of the remediation priority list with commits pushed by priority.
- Completed and pushed seven commits on `k8s-2025` `main` to GitHub (`origin` = `git@github.com:alanheinen/k8s-2025.git`):
  1. `7a4f1b9` — `docs: fix broken links and clarify canonical workflows`
  2. `eb73de0` — `docs: rewrite ansible README as operator guide`
  3. `a30badf` — `docs: add directory indexes for core repo areas`
  4. `5ca2bf1` — `docs: add operations indexes and service reference map`
  5. `388f129` — `docs: refactor recovery guides and add recovery indexes`
  6. `12c166f` — `docs: label current guides and historical summaries clearly`
  7. `d05f63b` — `docs: add archive index and label historical phase summaries`
- The documentation pass materially improved repo hygiene:
  - broken Markdown links were reduced from 19 to 0
  - root/docs/readme flow now explicitly states Ansible is the primary operating model, Terraform is secondary for provisioning, and older cloud-init/script workflows are legacy/alternate
  - `ansible/README.md` was rewritten from milestone-style prose into an operator-focused guide
  - added directory indexes for `terraform/`, `kubernetes/`, `operations/`, `docs/reference/`, `docs/architecture/`, `recovery/`, and service-recovery areas
  - added `docs/reference/services.md` as a service map
  - recovery docs were reframed as honest runbooks rather than overconfident “complete rebuild” claims
  - archive docs now carry historical-status banners instead of masquerading as live runbooks
- Follow-up static consistency check across bash/Ansible/Terraform found the overall model is mostly coherent but not fully consistent yet:
  - real bug: `scripts/deploy-homeassistant-pxe.sh` still uses old Terraform target `proxmox_vm_qemu.pxe` even though the current provider/resource is `proxmox_virtual_environment_vm.pxe`
  - stale old Terraform target names also remain in several docs, notably `docs/guides/homeassistant-pxe-deployment.md`, `docs/guides/clawdbot-deployment.md`, `ansible/roles/bastion_server/README.md`, and `docs/architecture/k8s.md`
  - bash syntax check passed, but `ansible-playbook` and `terraform` binaries were not installed in the current environment, so no live syntax/validate execution was possible
