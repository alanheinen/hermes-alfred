# 2026-04-05

## k8s-2025 Reorg Planning (5:00 AM CDT)

### Daily status report
- **What was reviewed today:** `k8s-2025/docs/README.md`, root `docs/` file inventory, `k8s-2025/ansible/playbooks/README.md`, and the current `ansible/playbooks/` layout.
- **What changed in the planning docs:**
  - Updated all four planning docs to 2026-04-05.
  - Added a validation/refinement pass confirming that `docs/` root is now tightly bounded and no longer a structural concern.
  - Documented that `ansible/playbooks/README.md` is now the clearest remaining documentation mismatch: it still presents the area as maintenance-centric even though the live tree is intent-split across `bootstrap/`, `deploy/`, `operate/`, and `recover/`.
  - Tightened the proposal and transition guidance so the next cleanup wave stays tiny: classify the two `docs/` root holdouts, then rewrite the Ansible playbooks README as an intent-based index, then stop.
  - Added naming guidance that human-facing labels and README framing now matter more than further filename churn.
- **Current milestone status:**
  - Phase 1 inventory/classification: complete enough for planning.
  - Phase 2 pain points/overlaps: complete enough; remaining issues are documentation alignment, not structure.
  - Phase 3 target structure: complete and already reflected in the repo.
  - Phase 4 naming conventions: mature; remaining work is mostly README/index labeling.
  - Phase 5 transition sequence: effectively complete; only a tiny doc-only cleanup wave remains if desired.
- **Open questions / blockers:**
  1. Should `docs/network-audit-process.md` ultimately live in `docs/guides/` or `operations/runbooks/`?
  2. Should `docs/opnsense-dns-dhcp-recommendations.md` live in `operations/audits/` or `docs/reference/`?
  3. Does Al want that final doc-only cleanup wave, or is the current structure "done enough"?
- **Next recommended steps:**
  1. classify the two remaining `docs/` root holdouts
  2. update `docs/README.md` only if those moves happen
  3. rewrite `ansible/playbooks/README.md` into a proper index for the current intent-based structure
  4. stop unless a real operator-facing discoverability issue appears
- **Proposal ready to present to Al?:** yes. It is ready now; remaining work is small refinement, not architecture design.

---

## Afternoon Ops / Repo Cleanup (2:24 PM CDT)

- Al confirmed `k8s6` was fixed manually after the earlier SSH reachability issue; subsequent network scan showed `k8s6` reachable again.
- NAS incident context from Al: `nas.lan` went down after trying the new Gemma4 model in Open WebUI / Ollama. The River 3 power station shut AC output off due to power draw. NAS was left not booting for unclear reasons; likely next triage should start with post-power-event recovery rather than blaming the model itself.
- Quant cron cleanup completed:
  - `quant-meeting-daily` prompt now explicitly requires the repo venv Python (`siloed-quant-repo/.venv/bin/python`) and forbids system-python fallback / ad-hoc monkeypatch self-repair.
  - Prompt also tightened to reduce noisy edits (especially `issue_tracker.log`) and to write reports in plain English for a non-finance reader.
- `k8s-2025` doc cleanup decisions from Al were executed:
  - `docs/network-audit-process.md` moved to `docs/guides/network-audit-process.md`
  - `docs/opnsense-dns-dhcp-recommendations.md` moved to `docs/reference/opnsense-dns-dhcp-recommendations.md`
  - `docs/README.md` rewritten to reflect the cleaned structure
  - `ansible/playbooks/README.md` rewritten as an index for the intent-based layout (`bootstrap/`, `deploy/`, `operate/`, `recover/`)
  - Al said the reorg process can stop after this cleanup unless a real discoverability problem appears later.
- Additional naming cleanup requested by Al:
  - `ARCHITECTURE.md` moved to `docs/architecture/k8s.md`
  - live reference in `ansible/playbooks/deploy/kubernetes.yml` updated accordingly.
- Network inventory update:
  - Added `Legion.lan` / `172.16.1.16` to `k8s-2025/docs/reference/network-inventory.yaml`
  - Al clarified it is a Windows laptop; inventory updated to `type: windows-laptop`, `os: Windows`.
