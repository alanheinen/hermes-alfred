# 2026-04-03 — Daily Memory Review (Automated, 1:00 AM CDT)

## Memory Maintenance Summary

**Recent daily logs reviewed:** 2026-03-31.md, 2026-04-01.md, 2026-04-02.md
- **Mar 31:** Quant work pivoted toward stricter short-term research; SSH/PAT/backup secret cleanup became the main durable ops story.
- **Apr 1:** `k8s-2025` reorg planning docs under `plans/` remain the durable repo-planning milestone; repo divergence warning from Mar 28 is stale.
- **Apr 2:** New long-term items were the announce-mode cron delivery misconfiguration (`delivery.to` missing), unresolved secret-hygiene follow-up items, stronger `k8s-2025` reorg findings, network inventory delta notes, and the updated short-term quant leader (`pullback_trend_reentry`).

**Older daily logs checked for purge/review status (older than 7 days):** 2026-03-16.md, 2026-03-18.md, 2026-03-19.md, 2026-03-20.md, 2026-03-22.md, 2026-03-23.md, 2026-03-24.md, 2026-03-25.md, 2026-03-26.md
- All already contain review markers or were previously confirmed as distilled into `MEMORY.md`.
- No missing long-term facts found.

**MEMORY.md updates:**
- ✅ Added the **Apr 2 cron delivery caveat**: announce-mode Telegram jobs can show `not-delivered` if `delivery.to` is missing.
- ✅ Added unresolved **secret-hygiene follow-up** items: local OpenClaw config/auth copies and the exposed PBS token note.
- ✅ Refreshed **k8s-2025 reorg planning** status with the newer concrete findings and the note that the proposal still needs a move map before it is presentation-ready.
- ✅ Added the latest **network inventory** signal (`Legion.lan` and `k8s6.lan` SSH issue).
- ✅ Updated the **short-term quant** note so `pullback_trend_reentry` replaces the older best-candidate mention.
- ✅ Trimmed/reworded recent updates to reduce duplication while keeping the durable bits.

**Files marked as reviewed:**
- No new review markers needed; all daily logs older than 7 days were already marked or previously distilled.

**Files purged:** None
- No dated daily logs are older than 30 days yet.
- Earliest remaining dated daily log is `2026-03-16.md`; first deletions should begin in mid-April if nothing newer changes that plan.

**Status:** Memory is current through 2026-04-02. Long-term memory now reflects the active cron-delivery cleanup, current secret-hygiene loose ends, stronger `k8s-2025` planning status, and the latest modest short-term quant leader.

---

## k8s-2025 Reorg Planning (4:16 AM CDT)

### Daily status report
- **What was reviewed today:** targeted current-state inventory of the repo root, `README.md`, `ARCHITECTURE.md`, structure samples under `alfred/`, `config/cluster-config/`, `config/cluster-config/argocd-apps/`, `ansible/playbooks/`, the execution record at `docs/archive/reorg-execution-log-2026-04-02.md`, plus directory listings for `docs/`, `operations/`, `recovery/`, `config/`, and `ansible/playbooks/`.
- **What changed in the planning docs:**
  - Added a 2026-04-03 validation update to all four planning docs.
  - Corrected the baseline to reflect that the repo has already been substantially reorganized.
  - Shifted the posture from designing a target structure to validating the executed structure and identifying residual cleanup.
  - Marked the proposal as presentation-ready as an explanation of the current structure and rationale.
  - Updated the transition plan to note that most planned stages were already executed on 2026-04-02.
  - Updated naming guidance to reflect that directory-level cleanup landed, while some filename-level normalization was intentionally deferred.
- **Current milestone status:**
  - Phase 1 inventory/classification: effectively complete for planning purposes.
  - Phase 2 pain points/overlaps: mostly complete; remaining issues are residual inconsistencies rather than major structural confusion.
  - Phase 3 target structure: complete enough; the target structure largely matches the live repo.
  - Phase 4 naming conventions: solid draft, partially adopted; further cleanup should be selective, not compulsive.
  - Phase 5 transition sequence: largely complete in practice because most major moves already landed on 2026-04-02.
- **Open questions / blockers:** mostly reduced to residual cleanup choices rather than architecture uncertainty.
- **Next recommended steps:**
  1. use the planning set as validation / explanation docs rather than as a greenfield redesign
  2. identify only the highest-value residual cleanup items before making more moves
  3. avoid filename-normalization churn unless it improves discoverability or consistency materially
- **Proposal ready to present to Al?:** yes. The current set is now presentation-ready as a grounded explanation of the repo structure and why it looks that way.

---

## Network Inventory Delta (5:31 AM CDT)

### Daily status report
- **New or changed hosts:**
  - `Legion.lan` / `172.16.1.16` was seen again, is still not SSH-reachable, and still looks like an unmanaged workstation/client showing up in the ARP neighbor table rather than a confirmed inventory host.
  - `k8s6.lan` / `172.16.1.206` is still not SSH-reachable even though it is listed in inventory as a Kubernetes worker expected to expose SSH on 22.
- **Known important reachability changes:**
  - `k8s6.lan` still resolves and pings, but TCP/22 did not answer.
  - Everything else checked looked unchanged: `opnsense.lan`, `vm01.lan`, `dsp01.lan`, `frigate.lan`, `k8s1-5`, `nas.lan`, `homeassistant.lan`, `octopi.lan`, `one-five.lan`, `pxe.lan`, `pbs.lan`, and `proxy.lan` were pingable and SSH-reachable.
- **Job/config note:** this remains a lightweight local delta check using existing inventory, DNS, ARP neighbors, ping, and TCP/22 probes — useful for drift, not a full census. The earlier note about the stale inventory source path still stands: the usable file is `k8s-2025/docs/reference/network-inventory.yaml`.
- **Recommended follow-up if doing changes later:**
  1. if `k8s6` should expose SSH, inspect its `sshd` state or firewall path
  2. if `Legion.lan` keeps showing up, add it to inventory; otherwise treat it as a transient client
  3. update the job spec so it points at the actual inventory file and stops being clever in the wrong direction

---

## k8s-2025 Reorg Planning (5:02 AM CDT)

### Validation follow-up
- **What was reviewed today:** live repo structure at a targeted level rather than a full archaeology pass: top-level directories/files, `docs/` root files, `ansible/playbooks/` structure, `ansible/awx/job-templates.yml`, and `docs/archive/reorg-execution-log-2026-04-02.md`.
- **Specifically validated:**
  - the two docs still sitting at `docs/` root
  - whether AWX naming cleanup actually landed
  - whether remaining naming issues are now mostly filenames rather than active template names
- **What changed in the planning docs:**
  - clarified `docs/network-audit-process.md` as a guide/runbook candidate
  - clarified `docs/opnsense-dns-dhcp-recommendations.md` as a reference or audit-output candidate
  - noted that active AWX naming is now largely normalized; the old `Patch - Kubernetes Node` mismatch is gone
  - noted that remaining `Maintenance - ...` / `Ad-hoc - ...` strings in AWX config are commented examples, not active template names
  - framed the main remaining naming question as whether conservative playbook filenames like `backup.yml`, `restore.yml`, and `check_cluster.yml` are worth touching at all
  - documented `cloud-init-templates/`, `scripts/`, and `kubernetes/` as likely pragmatic top-level exceptions rather than neglected leftovers
- **Current milestone status:**
  - Phase 1 inventory/classification: complete enough for planning
  - Phase 2 pain points/overlaps: complete enough; now mostly residual exceptions and cleanup choices
  - Phase 3 target structure: complete and mostly reflected in the live repo
  - Phase 4 naming conventions: mature; AWX portion appears largely implemented, remaining work is selective
  - Phase 5 transition sequence: effectively complete; now in validation/refinement mode
- **Open questions / blockers:** final placement calls for the remaining `docs/` root files and whether conservative playbook filenames are worth renaming.
- **Next recommended steps:**
  1. decide final homes for the remaining `docs/` root files based on whether they function as runbooks, references, or audit outputs
  2. leave AWX names alone unless a real inconsistency reappears
  3. avoid renaming plain playbook filenames unless the gain in discoverability clearly outweighs the churn

---

## Quant Short-Term Validation (7:02 AM CDT)

### Daily status report
- **What changed today:** `short_term_strategy.py` was tightened so walk-forward validation is now a real rolling 5-year train / next-year test, instead of selecting the full-sample winner first and congratulating ourselves for the rigged outcome.
- **Artifacts regenerated:** `short_term_walkforward.csv` and `short_term_research_summary.csv`.
- **Current result:** `pullback_trend_reentry` is still the best in-sample rule at about 4.4% CAGR / 0.68 Sharpe since 2010, versus SPY around 13.4% CAGR / 0.82 Sharpe.
- **Honest verdict:** the stricter validation made the short-term track look weaker, not stronger. Rolling OOS average is about 2.4% CAGR / 0.56 Sharpe versus SPY around 14.5% CAGR / 1.15 Sharpe. Useful correction; not a usable edge yet.
- **New ideas:** none today; the effort went into making the existing validation less fake.
- **Rotation strategy:** quick sanity check only; latest rotation artifacts do not look obviously broken and there was no material change.
- **Repo status:** committed and pushed as `ffa5e1b` — `research: make short-term walk-forward selection honest`.
- **Environment note:** `python3` lacked `numpy`; reran with the repo virtualenv and completed successfully.

---

## Failures / Attention Needed (8:32 AM CDT)

### Daily status report
- **Cron delivery remains flaky at the config layer:** announce-mode Telegram jobs are still missing explicit `delivery.to` targets, so confirmation is unreliable. Today the symptom leaned toward `lastDeliveryStatus: unknown` rather than the earlier `not-delivered`, which is the same problem wearing a slightly different hat.
- **Handled job errors:**
  - `quant-meeting-daily` logged `python-missing-numpy-system-env`, then fell back and completed. Best cleanup is to call `/home/aheinen/.openclaw/workspace/siloed-quant-repo/.venv/bin/python` directly.
  - `network-inventory-scan-daily` logged one handled shell-formatting error, then completed successfully. Best cleanup is to fix the shell formatting so the job stops heckling itself every morning.
- **Duplicate-run oddities:** `k8s-2025-reorg-planning-daily`, `network-inventory-scan-daily`, and `quant-backtest-daily` each show two runs today at old/new times. That points more toward schedule churn or stale run state than an actual missed run.
- **Open high-severity cleanup items still unchanged:** local `openclaw.json` plus backup copies, live `auth-profiles.json`, and the exposed PBS token in the `k8s-2025` docs set remain unresolved.
- **Pending review:** `daily-review-and-optimize` had not run yet at reminder time, so any additional findings were still pending.
- **Quant follow-up:** the important durable change is not a new winner but a more honest test — real rolling 5-year train / next-year test. That made short-term results weaker, not stronger; `pullback_trend_reentry` remains the best in-sample candidate, but still not a usable edge.
- **Network follow-up:** `Legion.lan` / `172.16.1.16` showed up again and still looks like an unmanaged or transient client, while `k8s6.lan` remains pingable but closed or unreachable on TCP/22 despite expectations.
- **k8s reorg note:** no contradiction to the earlier morning update; planning remains in validation/refinement mode rather than active redesign.
- **Recommended next fixes when doing changes with approval:**
  1. add explicit Telegram `delivery.to` targets to announce-mode jobs or replace them with explicit message sends
  2. pin `quant-meeting-daily` to the repo virtualenv Python
  3. clean the shell formatting in `network-inventory-scan-daily`
  4. review cron schedule history / recent edits so only the intended schedules remain active
  5. rotate or scrub the exposed OpenClaw / Anthropic / PBS secrets instead of merely admiring them from a safe distance
